seed: 42

data:
  num_trajs: 64
  num_steps: 100
  dt: 0.05

physics:
  gm: 1.0
  lambda_energy: 0.1
  lambda_angular: 0.1

training:
  num_epochs: 5000
  batch_size: 64
  learning_rate: 1.0e-3

model:
  hidden_dim: 128
  num_blocks: 3